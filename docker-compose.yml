version: '3.9'

services:
  backend:
    build:
      context: ./backend
    container_name: video-bot-backend
    ports:
      - '8000:8000'
    env_file:
      - ./backend/.env
    volumes:
      - ./backend/data:/app/data
      - ./backend/milvus.db:/app/milvus.db
